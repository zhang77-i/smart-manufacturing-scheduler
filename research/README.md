# 全规模实验代码

此目录保留生成聚合结果的完整多起点启发式、小规模 CP-SAT 对照和消融入口。

运行这些脚本需要与案例字段结构一致的三个本地工作簿：工单基础信息表、生产加工时间表、钻机最早可用时间表。原始表格与题目文档不公开，因此默认的 GitHub 快速验证使用 examples 下的合成实例。

核心算法包括 EDD、Release、Slack、CR 与随机多起点排序，机器时间轴最早可行插入，关键迟交订单换机与两订单局部重排。exact_cp_sat_benchmark.py 只在确定性小规模子集上做精确对照，不把小实例 gap 外推到 887 单全规模结果。
