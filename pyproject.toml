[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "heterogeneous-scheduler"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["ortools>=9.10,<10"]

[tool.pytest.ini_options]
pythonpath = ["src"]
